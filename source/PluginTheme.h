// SPACE's own colour accessors, included inside namespace Celine::Theme by ui/Theme.h.
//
// There are none: everything this window draws already has a name in the shared kit.
// The file exists because ui/Theme.h includes it unconditionally, which is what keeps
// that header identical across the house.

